#!/usr/bin/python
# -*- coding: UTF-8 -*-

import MySQLdb
import sys

"""
try:
    conn = MySQLdb.connect(db = "None",
                            host = "localhost",
                            user = "root",
                            passwd = "password")

    print "connected!"

except MySQLdb.Error, err:
    str_out = "cannot connect to server, error: %d, error message: %s" err.args[0], err.args[1]
    print str_out
    sys.exit(-1)

conn.close()
print "disconnected!"
"""

print PYTHONPATH
